//import { /* import ACTION_TYPES*/ } from "../actions";

const initialState = {
    logginIn: false
};

export const loginReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};